# Lucoze Website — UAT deployment environment
# ============================================
# Copy to /tmp/lucoze-website-uat.env, fill in values, then generate the compose:
#
#   docker compose \
#     --env-file /tmp/lucoze-website-uat.env \
#     -f deploy/lucoze-website-uat.yaml \
#     config > /tmp/lucoze-website-uat-final.yaml
#
# Upload /tmp/lucoze-website-uat-final.yaml to Dokploy UI (UAT-Lucoze-Website project).
# NEVER commit the real env file — it contains infrastructure details.
# ---------------------------------------------------------------------------

CUSTOM_IMAGE=svasamm/lucoze-website
CUSTOM_TAG=uat-latest
PULL_POLICY=always

# Agent VPS private IP — find in Hetzner Cloud Console → VPS → Network tab
AGENT_PRIVATE_IP=CHANGE_ME

# Port exposed on the agent's private network (must be unique per bench on same agent)
BENCH_PORT=CHANGE_ME
